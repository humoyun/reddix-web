query Post($id: String!) {
  post(id: $id) {
    id
    title
    type
    text
    html
    points
    flair
    mediaUrl
    linkPreview
    owner {
      id
      username
    }
    createdAt
    updatedAt
  }
}
