query Posts($limit: Int!, $cursor: String) {
  posts(limit: $limit, cursor: $cursor) {
    hasMore
    posts {
      id
      title
      type
      linkPreview
      flair
      points
      createdAt
      owner {
        id
        username
      }
    }
  }
}
