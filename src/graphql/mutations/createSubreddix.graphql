mutation CreateSubreddix($name: String!) {
  createSubreddix(name: $name) {
    errors
    data {
      id
      name
      flairs
      rules
      ownerId
      createdAt
      updatedAt
    }
  }
}
