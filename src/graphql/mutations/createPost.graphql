mutation CreatePost($input: PostInput!) {
  createPost(input: $input) {
    errors {
      field
      message
    }
    post {
      id
      title
      text
      type
      mediaUrl
      linkPreview
      points
      ownerId
      createdAt
      updatedAt
    }
  }
}
