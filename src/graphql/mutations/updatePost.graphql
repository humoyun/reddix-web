mutation UpdatePost($id: number, $title: String!, $text: String!) {
  updatePost($id: $id, title: $title, text: $text) {
    id
    title
    text
    points
    creatorId
    createdAt
    updatedAt
  }
}