mutation Login($username: String!, $password: String!) {
  login(args: { username: $username, password: $password }) {
    errors {
      field
      message
    }
    member {
      ...RegularMember
    }
  }
}
